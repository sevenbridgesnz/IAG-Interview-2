The updated solutions are in 'backend-updated' and 'frontend-updated'

I have primarily tested the API running under Kestrel, therefore port 5001 (dev certifacte must be trusted with 'dotnet dev-certs https --trust')

For demonstration, the Selenium tests are not headless.